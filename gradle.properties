# Required because the app depends on AndroidX libraries.
android.useAndroidX=true
android.nonTransitiveRClass=true
# Give the Gradle daemon enough heap on CI.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
